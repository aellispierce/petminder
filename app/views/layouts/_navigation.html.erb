<header class="navigation" role="banner">
  <div class="navigation-wrapper">
    <nav role="navigation">
      <ul id="js-navigation-menu" class="navigation-menu show">
        <li class="nav-link"><%= link_to "Home", root_path %></li>
        <li class="nav-link"><%=link_to "Add Pet", new_pet_path if session[:user_id] %></li>
        <li class="nav-link"><%=link_to "Add Task", new_task_path if session[:user_id] %></li>

          </ul>
        </li>
      </ul>
    </nav>
    <div class="navigation-tools">
      <%=link_to "Logout",sessions_logout_path, :class => "sign-up" if session[:user_id] %>
      <%= link_to "Sign Up", new_user_path, :class => "sign-up" if session[:user_id]==nil%>
    </div>
  </div>
</header>
